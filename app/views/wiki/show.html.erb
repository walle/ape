<h1><%= @wiki.page %></h1>

<%= render 'contents' %>

<%= render :partial => 'comments' %>

<h2><%= t(:new_comment) %></h2>
<%= form_tag comments_url(:project_id => params[:project_id], :type_id => params[:id].to_s, :type => :wiki) do |f| %>
  <ul>
    <li>
      <%= label_tag :comment %><br />
      <%= text_area_tag :comment %>
    </li>
    <li>
      <%= submit_tag t(:new_comment) %>
    </li>
  </ul>
<% end %>

