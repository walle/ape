<h1><%= t(:pages) %></h1>

<%= render 'menu', :no_edit => true %>

<% if(@pages.length > 0) %>
  <ul>
  <% @pages.each do |page| %>
    <li>
      <%= link_to page, wiki_page_url(:id => page) %>
    </li>
  <% end %>
  </ul>
<% else %>
  <p><%= t(:there_are_no_sub_pages) %></p>
<% end %>

