<%= link_to t(:edit), edit_ticket_url %>
<%= button_to t(:delete), ticket_path, :method => :delete, :confirm => t(:are_you_sure) %>

<%= gravatar_for @ticket %> - <%= @ticket.date %>

<%= rewrite_wiki_links @ticket %>

